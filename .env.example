# Owner roster at /crimson/activity
# Shared code Addison types to unlock the kit list. Change this before shipping pouches.
CRIMSON_OWNER_KEY=change-me-crimson-owner

# Service-role key from the existing Supabase project (Settings → API).
# Required for the activity API to read crimson_events (RLS blocks anon SELECT).
# Never expose this in the browser or commit it.
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_URL=https://exxpaetnxamzqifqkyly.supabase.co
